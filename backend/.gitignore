# 의존성
node_modules

# ── 환경변수 / 시크릿 (절대 커밋 금지) ──────────────────────────────────────
.env
.env.local
.env.*.local
.env.production
.env.staging

# Firebase 서비스 계정 키
serviceAccountKey.json
*serviceAccount*.json

# 기타 시크릿
*.pem
*.key
secrets/